---
phase: 02-lead-intelligence-and-site-templating
plan: 04
type: execute
wave: 1
depends_on: [02-02]
files_modified: [templates/src/lib/usePersonalization.ts, templates/src/app/*/page.tsx]
autonomous: true
requirements: [SITE-01, SITE-02]

must_haves:
  truths:
    - "All site templates support AI-generated copy via URL parameters"
    - "Fallbacks are implemented for all dynamic fields"
    - "Code duplication is minimized through a shared hook or component"
  artifacts:
    - path: "templates/src/lib/usePersonalization.ts"
      provides: "Shared hook for template data extraction and formatting"
---

<objective>
Refactor all Next.js site templates to utilize the advanced personalization library and support AI-generated copy.
The goal is to ensure consistency across all 30+ niche templates and make them ready for the AI-enriched lead pipeline.

Purpose: Scalable personalization across all supported niches.
Output: Refactored template pages using a centralized personalization hook.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Create Personalization Hook</name>
  <files>templates/src/lib/usePersonalization.ts</files>
  <action>
    - Implement `usePersonalization` hook in `templates/src/lib/usePersonalization.ts`.
    - The hook should use `useSearchParams` to extract `leadData` and `aiCopy`.
    - Provide a `t(templateString, defaults)` function that calls `formatPersonalizedContent`.
  </action>
  <done>Centralized hook for personalization is ready for template integration.</done>
</task>

<task type="auto">
  <name>Task 2: Batch Refactor Templates</name>
  <files>templates/src/app/*/page.tsx</files>
  <action>
    - Update all template files (`templates/src/app/*/page.tsx`) to use the `usePersonalization` hook.
    - Replace standard `formatContent` calls with the new `t` function from the hook.
    - Ensure `Suspense` is used correctly in all templates as required by `useSearchParams`.
  </action>
  <done>All 30+ niche templates now support deep AI personalization.</done>
</task>

</tasks>

<verification>
- Manually check 3-4 random templates (e.g., aqua, aura, industrial) with AI URL parameters.
- Verify that fallbacks work when no parameters are provided.
</verification>

<output>
After completion, create `.planning/phases/02-lead-intelligence/02-04-SUMMARY.md`
</output>
