---
phase: 02-lead-intelligence-and-site-templating
plan: 01
type: execute
wave: 1
depends_on: [03-google-maps-lead-intelligence]
files_modified: [templates/src/lib/personalization.ts, src/scrapers/export.py, templates/src/app/page.tsx]
autonomous: true
requirements: [LEAD-02, SITE-01, SITE-02]

must_haves:
  truths:
    - "Lead CRM accurately displays 'no-website' status from scraper exports"
    - "Next.js templates can dynamically render AI-generated copy if provided"
    - "Personalization logic handles fallbacks for missing data points"
  artifacts:
    - path: "templates/src/lib/personalization.ts"
      provides: "Advanced content formatting with AI copy support"
    - path: "templates/src/app/page.tsx"
      provides: "Updated CRM with enhanced lead status and previewing"
---

<objective>
Enhance the Lead CRM and Site Templating engine to support deep personalization and automated lead ingestion. 
The goal is to move from basic placeholder replacement to a system that can handle enriched data (AI copy, specific lead pain points).

Purpose: Increase the 'wow factor' of the sample sites by showing businesses we truly understand them.
Output: Updated Next.js CRM and an enhanced personalization library.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-google-maps-lead-intelligence/03-03-SUMMARY.md

# Interfaces (Planned)
- `formatPersonalizedContent(template: string, leadData: dict, aiCopy?: dict) -> string`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Advanced Personalization Library</name>
  <files>templates/src/lib/personalization.ts</files>
  <action>
    - Create a new utility library `templates/src/lib/personalization.ts` that extends `formatContent`.
    - Support complex objects for `aiCopy` (e.g., custom hero titles, pain point descriptions).
    - Implement robust fallbacks: if AI copy is missing, use niche-specific defaults; if niche is missing, use generic business defaults.
    - Add support for transforming data (e.g., capitalize niche names, format phone numbers).
  </action>
  <verify>
    <automated># Manual verification via Next.js dev server or unit tests if available</automated>
  </verify>
  <done>Templates now support deeply personalized AI-generated content with reliable fallbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Lead CRM UX Enhancements</name>
  <files>templates/src/app/page.tsx</files>
  <action>
    - Update the `LeadCRM` component to better handle the "no-website" leads from Phase 3.
    - Add a "Lead Quality" score indicator based on ratings and review counts.
    - Improve the "Generate Site" preview logic to automatically select the best template based on the niche keyword in the filename.
    - Add a "Copy Sample Link" button to easily share the personalized URL.
  </action>
  <verify>
    <automated># Visual verification in the browser</automated>
  </verify>
  <done>CRM is now optimized for processing Phase 3 leads with improved preview orchestration.</done>
</task>

<task type="auto">
  <name>Task 3: Scraper-CRM Integration Refinement</name>
  <files>src/scrapers/export.py</files>
  <action>
    - Update `ExcelExporter` to include a `is_lead` or `no_website` flag in the exported data for easier CRM filtering.
    - Ensure all extracted details (address, phone, reviews) are consistently mapped to the Excel headers expected by the CRM.
  </action>
  <verify>
    <automated>python -m src.main --niche "test" --location "test" --max-results 1 && # check xlsx headers</automated>
  </verify>
  <done>Scraper exports are perfectly aligned with CRM data requirements.</done>
</task>

</tasks>

<verification>
- Verify that a lead from a Phase 3 export appears correctly in the CRM.
- Confirm that clicking "Generate Site" opens a personalized page with correct data.
- Test fallback logic by removing certain parameters from the URL.
</verification>

<success_criteria>
- CRM successfully loads and displays leads from the latest scraper runs.
- Site templates show correct business name, niche, and location.
- Personalization library is modular and ready for Phase 2's AI copy integration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-lead-intelligence/02-01-SUMMARY.md`
</output>
