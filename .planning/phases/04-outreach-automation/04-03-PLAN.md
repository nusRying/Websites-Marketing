---
phase: 04-outreach-automation-and-visual-proof
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [templates/src/app/api/track/route.ts, src/slack_alerts.py, templates/src/lib/usePersonalization.ts]
autonomous: true
requirements: [OUT-03]

must_haves:
  truths:
    - "Site views are recorded with lead identity and timestamp"
    - "Slack notifications are sent in real-time when a lead visits their site"
    - "Tracking is non-intrusive and does not slow down page load"
  artifacts:
    - path: "templates/src/app/api/track/route.ts"
      provides: "Lead engagement tracking endpoint"
---

<objective>
Implement real-time lead engagement tracking and Slack alerts.
The goal is to notify the sales team the exact moment a lead is viewing their sample website, allowing for perfectly timed follow-up.

Purpose: High-intent signal capture for 'strike while the iron is hot' sales.
Output: Tracking API, Slack integration, and frontend telemetry.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Engagement Tracking API</name>
  <files>templates/src/app/api/track/route.ts</files>
  <action>
    - Create a Next.js API route `api/track` to receive view events.
    - Logic to:
      - Log the visit to `src/data/crm_states.json` under the lead's history.
      - Capture metadata: IP (basic), User Agent, and Timestamp.
  </action>
  <done>Backend can now record lead visits and engagement history.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend Telemetry Integration</name>
  <files>templates/src/lib/usePersonalization.ts</files>
  <action>
    - Update the `usePersonalization` hook to trigger a tracking event on component mount.
    - Ensure it only tracks if a 'name' and 'phone' (lead ID) are present in the URL.
  </action>
  <done>Sample sites now automatically report views back to the CRM.</done>
</task>

<task type="auto">
  <name>Task 3: Real-time Slack Alerts</name>
  <files>src/slack_alerts.py</files>
  <action>
    - Implement a small utility `src/slack_alerts.py` to send formatted messages to a Slack webhook.
    - Trigger this alert from the `api/track` route.
    - Format: "🔥 LEAD VIEWING: {Business Name} is looking at their sample site right now! [Link to CRM]"
  </action>
  <done>The sales team receives instant notifications of lead interest.</done>
</task>

</tasks>

<verification>
- Manually visit a personalized sample site URL.
- Verify that the visit appears in `crm_states.json`.
- Verify that a Slack notification is triggered (mocked if no webhook provided).
</verification>

<output>
After completion, create `.planning/phases/04-outreach-automation/04-03-SUMMARY.md`
</output>
