---
phase: 04-outreach-automation-and-visual-proof
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/outreach_orchestrator.py, src/ai_enrichment.py, templates/src/app/page.tsx]
autonomous: true
requirements: [OUT-01, OUT-02]

must_haves:
  truths:
    - "Can generate a Smartlead-compatible CSV with all personalization tokens"
    - "Personalized URLs are correctly formatted for one-click tracking"
    - "Visual proof image paths are ready for bulk upload/hosting"
  artifacts:
    - path: "src/outreach_orchestrator.py"
      provides: "CSV generation and outreach data formatting"
---

<objective>
Orchestrate the transition from enriched data to outreach campaigns.
The goal is to automate the creation of Smartlead-ready datasets that include personalized site links, visual proof tokens, and AI-generated first lines.

Purpose: Seamless bridge between lead intelligence and email sending.
Output: Outreach orchestrator script and updated dashboard features.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Outreach Data Orchestrator</name>
  <files>src/outreach_orchestrator.py</files>
  <action>
    - Create `src/outreach_orchestrator.py` to handle data transformations for Smartlead/Instantly.
    - Implement logic to:
      - Clean phone numbers and names for email tokens.
      - Generate the final `PERSONALIZED_URL` for every lead.
      - Map `ai_` fields to Smartlead custom variables (e.g., `{{custom_first_line}}`).
      - Prepare a CSV export specifically formatted for Smartlead bulk import.
  </action>
  <done>Enriched lead data can now be exported in sending-ready formats.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard 'Export to Outreach' Action</name>
  <files>templates/src/app/page.tsx</files>
  <action>
    - Add an "Export to Smartlead CSV" button to the main dashboard.
    - This allows for manual batching of high-quality leads for outreach.
    - Ensure it pulls the latest edited AI copy from the sidebar state if available.
  </action>
  <done>The CRM now provides a direct path to start outreach campaigns from the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Pipeline Integration (Final Link)</name>
  <files>src/ai_enrichment.py</files>
  <action>
    - Update `src/ai_enrichment.py` to automatically call the `OutreachOrchestrator` after screenshots are done.
    - This creates a 'fully baked' campaign file for every batch processed.
  </action>
  <done>The pipeline now outputs a ready-to-send outreach file as its final artifact.</done>
</task>

</tasks>

<verification>
- Verify that the generated CSV imports correctly into a spreadsheet tool.
- Confirm all AI tokens and URLs are present and correctly formatted.
- Test the 'Export' button in the dashboard with a mock lead.
</verification>

<output>
After completion, create `.planning/phases/04-outreach-automation/04-02-SUMMARY.md`
</output>
