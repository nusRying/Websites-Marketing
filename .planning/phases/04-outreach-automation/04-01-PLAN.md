---
phase: 04-outreach-automation-and-visual-proof
plan: 01
type: execute
wave: 1
depends_on: [02-04]
files_modified: [src/screenshot_worker.py, src/pipeline_watcher.py, src/scrapers/export.py]
autonomous: true
requirements: [SITE-03]

must_haves:
  truths:
    - "Screenshot worker can render a dynamic Next.js page with URL parameters"
    - "High-quality screenshots are saved to a dedicated directory"
    - "The screenshot filename is unique and linkable to the lead"
  artifacts:
    - path: "src/screenshot_worker.py"
      provides: "Automated site preview capture logic"
---

<objective>
Implement the automated screenshot worker to capture visual proof of the sample websites.
The goal is to generate high-impact images of the personalized sites to include in cold emails, significantly increasing click-through rates.

Purpose: Visual 'Show, Don't Tell' for the sales process.
Output: Screenshot worker script and integration with the pipeline watcher.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Develop Screenshot Worker</name>
  <files>src/screenshot_worker.py</files>
  <action>
    - Create `src/screenshot_worker.py` using `playwright` (Python).
    - Implement a method `capture_site(url, output_path)` that:
      - Sets a realistic viewport (desktop and mobile options).
      - Waits for animations/Reveal components to finish (or forces them to show).
      - Captures a high-quality full-page or hero-section screenshot.
    - Add logic to handle local dev server URLs (e.g., http://localhost:3000/...).
  </action>
  <done>Worker can successfully capture beautiful previews of dynamic templates.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate with Pipeline Watcher</name>
  <files>src/pipeline_watcher.py</files>
  <action>
    - Update `src/pipeline_watcher.py` to trigger the screenshot worker after AI enrichment is complete.
    - Ensure screenshots are generated for every lead in an enriched file.
    - Organize screenshots in `exports/screenshots/{file_prefix}/` directory.
  </action>
  <done>The pipeline now automatically generates visual proof for every new lead enriched by AI.</done>
</task>

<task type="auto">
  <name>Task 3: Link Screenshots to Lead Data</name>
  <files>src/scrapers/export.py</files>
  <action>
    - Update the export logic to include a 'Screenshot Path' column in the Excel file.
    - This allows outreach tools (Smartlead/Instantly) to easily pull the correct image for each lead.
  </action>
  <done>Lead data now contains direct references to their personalized visual proof.</done>
</task>

</tasks>

<verification>
- Run the screenshot worker against a local Next.js page and verify the image quality.
- Verify that adding a new scraper export triggers the full Enrichment -> Screenshot flow.
- Confirm the final Excel file contains the correct screenshot paths.
</verification>

<output>
After completion, create `.planning/phases/04-outreach-automation/04-01-SUMMARY.md`
</output>
