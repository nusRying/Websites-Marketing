---
phase: 05-conversion-and-scaling
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/smartlead_api.py, templates/src/app/api/track/route.ts, templates/src/app/page.tsx]
autonomous: true
requirements: [OUT-01, OUT-03]

must_haves:
  truths:
    - "Can push leads to Smartlead campaigns via API"
    - "Leads are automatically escalated to a 'Hot' campaign upon viewing their site"
    - "Dashboard provides clear visibility into high-intent lead activity"
  artifacts:
    - path: "src/smartlead_api.py"
      provides: "Smartlead integration layer"
---

<objective>
Automate the escalation of high-intent leads to maximize conversion velocity.
The goal is to move beyond manual CSV exports and automatically push leads into priority follow-up sequences in Smartlead the moment they engage with their sample site.

Purpose: Eliminate follow-up lag and automate the 'Hot Lead' pipeline.
Output: Smartlead API integration and automated escalation logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Develop Smartlead API Wrapper</name>
  <files>src/smartlead_api.py</files>
  <action>
    - Create `src/smartlead_api.py` to interface with the Smartlead.ai API.
    - Implement methods to:
      - Add a lead to a specific campaign.
      - Update lead custom fields (to reflect site views).
      - Check if a lead already exists in a campaign.
  </action>
  <done>System can now communicate programmatically with the outreach engine.</done>
</task>

<task type="auto">
  <name>Task 2: Automated Escalation Trigger</name>
  <files>templates/src/app/api/track/route.ts</files>
  <action>
    - Update the tracking API to trigger an escalation event when a lead views their site.
    - Logic: If a lead views the site, call the Smartlead API to move them to a "Priority Reply" or "Hot Lead" campaign.
    - Log the escalation result back to the CRM history.
  </action>
  <done>The pipeline now reacts instantly to lead intent by updating the outreach sequence.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard 'Hot Leads' Feed</name>
  <files>templates/src/app/page.tsx</files>
  <action>
    - Implement a "Live Activity" or "Hot Leads" feed in the dashboard.
    - Show real-time site views and booking attempts.
    - Add a visual indicator (e.g., pulsing flame icon) for leads with recent activity.
  </action>
  <done>The sales dashboard now prioritizes leads based on real-time behavior.</done>
</task>

</tasks>

<verification>
- Mock a site view and verify the Smartlead API call is triggered (logged if no API key).
- Verify the 'Hot Leads' indicator appears correctly in the CRM table.
- Confirm the CRM history correctly records the escalation event.
</verification>

<output>
After completion, create `.planning/phases/05-conversion-and-scaling/05-02-SUMMARY.md`
</output>
