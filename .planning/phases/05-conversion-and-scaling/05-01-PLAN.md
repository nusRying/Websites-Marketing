---
phase: 05-conversion-and-scaling
plan: 01
type: execute
wave: 1
depends_on: [04-03]
files_modified: [templates/src/lib/usePersonalization.ts, templates/src/components/BookingWidget.tsx, templates/src/app/*/page.tsx]
autonomous: true
requirements: [SITE-05]

must_haves:
  truths:
    - "Calendly/Booking widget is functional on all sample sites"
    - "Booking links are dynamically configurable via URL parameters"
    - "The conversion path (from site view to booking) is clear and low-friction"
  artifacts:
    - path: "templates/src/components/BookingWidget.tsx"
      provides: "Reusable booking component"
---

<objective>
Implement direct meeting booking integration to maximize conversions.
The goal is to provide leads with an instant way to book a call directly from their personalized sample site, reducing the friction between interest and sales.

Purpose: High-conversion bottom-of-funnel optimization.
Output: Booking widget component and platform-wide template integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Create Reusable Booking Widget</name>
  <files>templates/src/components/BookingWidget.tsx</files>
  <action>
    - Create a reusable `BookingWidget` component that embeds a Calendly or TidyCal inline widget.
    - Support a `bookingUrl` prop that can be passed from the personalization hook.
    - Style it to be non-intrusive but high-visibility (e.g., in a dedicated 'Let's Chat' section).
  </action>
  <done>Reusable booking widget is ready for template integration.</done>
</task>

<task type="auto">
  <name>Task 2: Update Personalization Hook for Bookings</name>
  <files>templates/src/lib/usePersonalization.ts</files>
  <action>
    - Update `usePersonalization` to extract a `booking_url` (or `calendly`) from URL parameters.
    - Provide a default booking URL if none is provided.
  </action>
  <done>Booking data is now part of the core personalization flow.</done>
</task>

<task type="auto">
  <name>Task 3: Batch Integrate Booking Widget</name>
  <files>templates/src/app/*/page.tsx</files>
  <action>
    - Batch refactor all 30+ niche templates to include the `BookingWidget` in their footer or a new dedicated section.
    - Ensure the CTA button in the hero section can also scroll to the booking widget.
  </action>
  <done>Every sample site now features a direct path to book a meeting.</done>
</task>

</tasks>

<verification>
- Verify the booking widget renders correctly on a sample site.
- Test passing a custom booking URL via URL parameters.
- Confirm that the widget is properly styled and responsive across different niches.
</verification>

<output>
After completion, create `.planning/phases/05-conversion-and-scaling/05-01-SUMMARY.md`
</output>
