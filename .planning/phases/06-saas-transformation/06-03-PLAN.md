---
phase: 06-saas-transformation
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified: [Dockerfile, docker-compose.yml, src/api_worker.py, requirements.txt]
autonomous: true
requirements: [INF-SAAS-04]

must_haves:
  truths:
    - "Backend services (Scraper, AI, Screenshots) are containerized using Docker"
    - "Workers can be triggered via an HTTP API instead of just local filesystem events"
    - "All dependencies (Playwright, Scrapling, OpenAI) are correctly configured in the image"
  artifacts:
    - path: "Dockerfile"
      provides: "Production-ready backend image"
    - path: "src/api_worker.py"
      provides: "Cloud-triggerable API for pipeline jobs"
---

<objective>
Containerize the backend automation engine to enable cloud-native scaling.
The goal is to package the Python scraper, enrichment, and screenshot workers into a Docker image that can be deployed to any cloud provider, moving away from local-only execution.

Purpose: Scalability, deployment flexibility, and infrastructure-as-code.
Output: Docker configuration and an API-based job trigger.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerize the Python Backend</name>
  <files>Dockerfile, .dockerignore</files>
  <action>
    - Create a `Dockerfile` based on `python:3.11-slim`.
    - Install system dependencies for Playwright (browsers).
    - Copy the project source and install Python requirements.
    - Set up the environment for production execution.
  </action>
  <done>Backend engine is now portable and production-ready via Docker.</done>
</task>

<task type="auto">
  <name>Task 2: Implement API Job Worker</name>
  <files>src/api_worker.py</files>
  <action>
    - Create `src/api_worker.py` using `FastAPI`.
    - Implement an endpoint `/run-pipeline` that accepts:
      - `niche`, `location`, `max_results`, and `user_id`.
    - The endpoint should trigger the full pipeline (Scrape -> Enrich -> Screenshot -> Sync) in a background thread.
  </action>
  <done>The pipeline can now be triggered by the SaaS frontend via HTTP.</done>
</task>

<task type="auto">
  <name>Task 3: Docker Compose for Orchestration</name>
  <files>docker-compose.yml</files>
  <action>
    - Create a `docker-compose.yml` to orchestrate the API worker.
    - Configure environment variable passthrough for Supabase and OpenAI keys.
  </action>
  <done>Full backend stack can be launched with a single command.</done>
</task>

</tasks>

<verification>
- Build the Docker image: `docker build -t lead-engine-backend .`
- Run the container and trigger a job via `curl`.
- Verify the job completes and syncs data to the Supabase cloud.
</verification>

<output>
After completion, create `.planning/phases/06-saas-transformation/06-03-SUMMARY.md`
</output>
