---
phase: 06-saas-transformation
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified: [templates/src/app/api/billing/checkout/route.ts, templates/src/app/api/billing/webhook/route.ts, templates/src/app/page.tsx, templates/package.json]
autonomous: true
requirements: [INF-SAAS-05]

must_haves:
  truths:
    - "Users can subscribe to a monthly plan via Stripe"
    - "System blocks lead generation features if no active subscription is found"
    - "Subscription status is handled securely via Stripe webhooks"
  artifacts:
    - path: "templates/src/app/api/billing/checkout/route.ts"
      provides: "Stripe Checkout session generator"
---

<objective>
Implement Stripe billing to monetize the SaaS platform.
The goal is to integrate a subscription-based paywall that secures the lead generation features, ensuring the project functions as a viable commercial product.

Purpose: Monetization and access control.
Output: Stripe checkout flow, webhook listener, and subscription-guarded UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Stripe Integration & Checkout</name>
  <files>templates/package.json, templates/src/app/api/billing/checkout/route.ts</files>
  <action>
    - Install `stripe` and `@stripe/stripe-js` in templates.
    - Create `api/billing/checkout` to generate Stripe Checkout sessions.
    - Pass the Supabase `user_id` to Stripe as a `client_reference_id`.
  </action>
  <done>Subscription purchase flow is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Webhook Listener & Profile Sync</name>
  <files>templates/src/app/api/billing/webhook/route.ts, supabase/migrations/20260401_billing.sql</files>
  <action>
    - Create a database migration to add `subscription_status` and `stripe_customer_id` to the `profiles` table.
    - Implement `api/billing/webhook` to listen for `checkout.session.completed` and `customer.subscription.deleted` events.
    - Update the user profile in Supabase when billing events occur.
  </action>
  <done>User subscription status is automatically synced between Stripe and Supabase.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard Subscription Guard</name>
  <files>templates/src/app/page.tsx</files>
  <action>
    - Update the Lead CRM to check for an active subscription status.
    - If inactive, show a "Subscription Required" overlay or disable the "Export" and "Sync" buttons.
    - Add a "Manage Billing" button that links to the Stripe Customer Portal.
  </action>
  <done>Monetization layer is fully integrated into the user experience.</done>
</task>

</tasks>

<verification>
- Trigger a mock Stripe Checkout session.
- Use Stripe CLI to simulate a successful payment webhook.
- Verify the user's profile in Supabase is updated to 'active'.
- Confirm that dashboard features are unlocked only when active.
</verification>

<output>
After completion, create `.planning/phases/06-saas-transformation/06-04-SUMMARY.md`
</output>
