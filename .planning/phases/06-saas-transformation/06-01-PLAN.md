---
phase: 06-saas-transformation
plan: 01
type: execute
wave: 1
depends_on: [05-02]
files_modified: [templates/src/lib/supabase.ts, templates/src/app/api/auth/route.ts, templates/src/app/api/leads/route.ts, templates/src/app/api/crm/route.ts, templates/package.json]
autonomous: true
requirements: [INF-SAAS-01, INF-SAAS-02]

must_haves:
  truths:
    - "User authentication is implemented and required for dashboard access"
    - "Lead data is stored in Supabase (PostgreSQL) instead of local files"
    - "Row Level Security (RLS) is active to ensure multi-tenant data privacy"
  artifacts:
    - path: "templates/src/lib/supabase.ts"
      provides: "Cloud database client"
    - path: "supabase/migrations/20260401_init.sql"
      provides: "SaaS database schema"
---

<objective>
Migrate the local lead engine to a cloud-native SaaS architecture using Supabase.
The goal is to transition from local file-based storage to a multi-tenant PostgreSQL database with secure authentication, enabling the project to function as a scalable product.

Purpose: Scalability, multi-tenancy, and cloud accessibility.
Output: Integrated Supabase client, Auth flow, and Database migration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase Integration & Schema Design</name>
  <files>templates/src/lib/supabase.ts, supabase/migrations/20260401_init.sql</files>
  <action>
    - Install `@supabase/supabase-js` in the templates directory.
    - Create `templates/src/lib/supabase.ts` to initialize the client.
    - Design the SaaS database schema:
      - `profiles`: User profiles linked to auth.users.
      - `batches`: Metadata for scraper runs.
      - `leads`: Structured lead data (Name, Email, AI copy, etc.) with `user_id`.
      - `crm_history`: Tracking events (views, escalations) per lead.
    - Enable Row Level Security (RLS) on all tables.
  </action>
  <done>Cloud database infrastructure is provisioned and structured.</done>
</task>

<task type="auto">
  <name>Task 2: Next.js Auth Middleware & Login</name>
  <files>templates/src/middleware.ts, templates/src/app/login/page.tsx</files>
  <action>
    - Implement a simple login page using Supabase Auth.
    - Add Next.js Middleware to protect the dashboard (`/`) and API routes.
    - Ensure only authenticated users can access lead intelligence.
  </action>
  <done>The application is now secure and multi-tenant ready.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor API Routes for Cloud DB</name>
  <files>templates/src/app/api/leads/route.ts, templates/src/app/api/crm/route.ts, templates/src/app/api/track/route.ts</files>
  <action>
    - Refactor `api/leads` to fetch from the `leads` table (filtered by user_id) instead of reading local `.xlsx` files.
    - Refactor `api/crm` to update lead status in the database.
    - Refactor `api/track` to log visits to the `crm_history` table.
  </action>
  <done>The frontend is now completely decoupled from the local filesystem.</done>
</task>

</tasks>

<verification>
- Verify that the login page correctly authenticates a user.
- Confirm that leads added to the database appear in the dashboard.
- Verify that RLS prevents User A from seeing User B's leads.
- Test tracking an event and seeing it update in the cloud database history.
</verification>

<output>
After completion, create `.planning/phases/06-saas-transformation/06-01-SUMMARY.md`
</output>
