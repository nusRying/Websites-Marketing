---
phase: 06-saas-transformation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/cloud_sync.py, src/ai_enrichment.py, src/screenshot_worker.py, templates/src/app/api/proxy-image/route.ts]
autonomous: true
requirements: [INF-SAAS-03]

must_haves:
  truths:
    - "Scraper results are automatically synced to the Supabase cloud database"
    - "Screenshots are hosted in Supabase Storage buckets instead of local folders"
    - "Personalized site templates correctly load images from cloud storage"
  artifacts:
    - path: "src/cloud_sync.py"
      provides: "Local-to-Cloud data synchronization logic"
---

<objective>
Bridge the local automation engine with the cloud SaaS platform.
The goal is to automate the upload of lead data and visual proof assets to Supabase, ensuring that the cloud dashboard is always up-to-date with the latest scraper runs.

Purpose: Cloud availability and asset hosting.
Output: Cloud sync script and Supabase Storage integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Cloud Data Synchronizer</name>
  <files>src/cloud_sync.py</files>
  <action>
    - Create `src/cloud_sync.py` using `supabase-py`.
    - Implement logic to:
      - Create a new 'batch' record in Supabase.
      - Bulk insert leads from an AI-enriched Excel file.
      - Map local fields to Supabase column names.
    - Use environment variables for Supabase URL and Service Role Key.
  </action>
  <done>Local scraper results can now be pushed to the SaaS database.</done>
</task>

<task type="auto">
  <name>Task 2: Supabase Storage for Screenshots</name>
  <files>src/screenshot_worker.py</files>
  <action>
    - Update `ScreenshotWorker` to upload captured PNGs to a 'screenshots' bucket in Supabase.
    - Replace local file paths in the lead data with public Supabase Storage URLs.
    - Ensure bucket exists or handle missing bucket errors gracefully.
  </action>
  <done>Visual proof is now hosted in the cloud and accessible via public URLs.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the SaaS Pipeline</name>
  <files>src/ai_enrichment.py, templates/src/app/api/proxy-image/route.ts</files>
  <action>
    - Update `src/ai_enrichment.py` to trigger `CloudSync` as the final terminal action.
    - Update the `proxy-image` API route to handle both local and cloud URLs for backward compatibility during transition.
  </action>
  <done>The end-to-end pipeline now terminates in the SaaS cloud.</done>
</task>

</tasks>

<verification>
- Run a small scrape and verify leads appear in the Supabase Dashboard.
- Confirm screenshots are visible in the Supabase Storage bucket.
- Verify the Lead CRM shows cloud-hosted images correctly.
</verification>

<output>
After completion, create `.planning/phases/06-saas-transformation/06-02-SUMMARY.md`
</output>
