---
phase: 03-google-maps-lead-intelligence
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [src/main.py, src/scrapers/engine.py]
autonomous: false
requirements: [LEAD-GM-01, LEAD-GM-02, LEAD-GM-03]

must_haves:
  truths:
    - "A complete end-to-end run is successful"
    - "Results for 'Roofing contractors in London' are saved to Excel"
    - "Scraper handles G-Maps 'No results' or rate-limit messages gracefully"
  artifacts:
    - path: "src/main.py"
      provides: "The main entry point for the scraper CLI"
  key_links:
    - from: "src/main.py"
      to: "google_maps.py, details.py, export.py"
      via: "module imports"
---

<objective>
Finalize the scraper with a robust entry point and perform a Proof-of-Concept run.
The goal is to provide a user-friendly CLI and verify the system against a real-world query.

Purpose: Deliver the tool in a ready-to-use state for lead generation teams.
Output: Working CLI and a verified Excel lead list for 'Roofing contractors in London'.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-google-maps-lead-intelligence/03-01-SUMMARY.md
@.planning/phases/03-google-maps-lead-intelligence/03-02-SUMMARY.md

# Interfaces (Planned)
- `python src/main.py --search "keyword" --location "city" --output "leads.xlsx"`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orchestration & CLI Development</name>
  <files>src/main.py</files>
  <action>
    - Implement a main entry point script `src/main.py` using `argparse` or `click`.
    - Glue together the discovery, extraction, and export logic from previous plans.
    - Wire the search, filter, detail extraction, and Excel saving into a single flow.
    - Implement progress logging and summary output to the console.
  </action>
  <verify>
    <automated>python src/main.py --help</automated>
  </verify>
  <done>CLI script correctly orchestrates the full scraping process.</done>
</task>

<task type="auto">
  <name>Task 2: Stealth Tuning & Rate Limit Resilience</name>
  <files>src/scrapers/engine.py</files>
  <action>
    - Update the base `ScraperEngine` per D-05 to include randomized delays between actions.
    - Add user-agent rotation (if supported by Scrapling natively or via header customization).
    - Implement retry logic for network timeouts or intermittent Google Maps errors.
    - Add logic to handle "Are you a robot?" challenges (e.g., alert the user or implement a wait period).
  </action>
  <verify>
    <automated>python src/main.py --test-stealth</automated>
  </verify>
  <done>Scraper engine is resilient against rate limiting and detection.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Proof-of-Concept Run</name>
  <what-built>Full end-to-end scraper using Scrapling v0.4.1.</what-built>
  <how-to-verify>
    1. Run: `python src/main.py --search "Roofing contractors" --location "London" --output "exports/london_roofers.xlsx"`
    2. Monitor the console output for discovery and extraction progress.
    3. Verify that `exports/london_roofers.xlsx` is created and contains detailed leads.
    4. Confirm that at least 5 leads in the file indeed have no website link on Google Maps.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Run full scraper for a test query and check if results appear in Excel.
- Verify human verification of lead quality (no website).
</verification>

<success_criteria>
- Full CLI is functional.
- POC run returns >10 qualified leads for the London roofing niche.
- Excel file contains phone, address, and recent reviews.
</success_criteria>

<output>
After completion, create `.planning/phases/03-google-maps-lead-intelligence/03-03-SUMMARY.md`
</output>
