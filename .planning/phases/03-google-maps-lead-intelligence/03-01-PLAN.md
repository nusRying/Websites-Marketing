---
phase: 03-google-maps-lead-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, src/scrapers/google_maps.py, src/scrapers/engine.py]
autonomous: true
requirements: [LEAD-GM-01]

must_haves:
  truths:
    - "Scrapling (v0.4.1) can initialize a stealthy browser session"
    - "Searching for 'Roofing in London' returns raw business listings"
    - "Businesses with website links are filtered out from the discovery list"
  artifacts:
    - path: "src/scrapers/google_maps.py"
      provides: "Google Maps search and discovery logic"
    - path: "requirements.txt"
      provides: "Project dependencies including Scrapling v0.4.1"
  key_links:
    - from: "src/scrapers/google_maps.py"
      to: "Google Maps Search URL"
      via: "Scrapling fetch/goto"
---

<objective>
Implement the core Google Maps search and discovery engine using Scrapling (v0.4.1). 
The goal is to search for a niche + location and identify businesses that do NOT have a website link.

Purpose: Automate lead ingestion for businesses with high sales potential (no website).
Output: A list of business listing URLs/IDs filtered for "no website".
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Interfaces (Planned)
- `GoogleMapsScraper.search(query: str, location: str) -> List[dict]`
- `GoogleMapsScraper.filter_no_website(results: List[dict]) -> List[dict]`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment Setup & Scrapling Integration</name>
  <files>requirements.txt, src/scrapers/engine.py</files>
  <action>
    - Create a `requirements.txt` file specifying `scrapling==0.4.1`, `pandas`, and `openpyxl`.
    - Implement a base `ScraperEngine` class in `src/scrapers/engine.py` that initializes a Scrapling stealth browser session per D-05.
    - Ensure Scrapling is configured for high stealth to avoid Google Maps bot detection.
  </action>
  <verify>
    <automated>pip install -r requirements.txt && python -c "from scrapling import Fetcher; print('Scrapling loaded')"</automated>
  </verify>
  <done>Dependencies installed and Scrapling engine initialized.</done>
</task>

<task type="auto">
  <name>Task 2: Search & Discovery with Website Filtering</name>
  <files>src/scrapers/google_maps.py</files>
  <action>
    - Implement `GoogleMapsScraper` in `src/scrapers/google_maps.py` using the `ScraperEngine`.
    - Create a `search` method that navigates to Google Maps and enters the query (e.g., '{niche} in {location}').
    - Implement infinite scroll/pagination logic to discover businesses.
    - Add a filtering step during discovery that identifies listings *without* a website button/link.
    - Return a list of candidate business objects (name and listing URL).
  </action>
  <verify>
    <automated>python src/scrapers/google_maps.py --test-search "Roofing in London" --filter-no-website</automated>
  </verify>
  <done>Scraper successfully finds and filters businesses without websites.</done>
</task>

</tasks>

<verification>
- Run discovery script and verify it returns >0 leads for a common niche.
- Manually check 2-3 filtered leads on Google Maps to ensure they actually have no website link.
</verification>

<success_criteria>
- Scrapling v0.4.1 is functional and bypasses initial Google Maps blocks.
- Search returns a filtered list of businesses without websites.
</success_criteria>

<output>
After completion, create `.planning/phases/03-google-maps-lead-intelligence/03-01-SUMMARY.md`
</output>
