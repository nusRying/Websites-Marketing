---
phase: 03-google-maps-lead-intelligence
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/scrapers/details.py, src/scrapers/export.py]
autonomous: true
requirements: [LEAD-GM-02, LEAD-GM-03]

must_haves:
  truths:
    - "Scraper can visit a business detail page on Google Maps"
    - "Phone number and address are extracted correctly"
    - "Recent reviews are captured as part of business data"
    - "Final results are saved to an .xlsx file with correct headers"
  artifacts:
    - path: "src/scrapers/details.py"
      provides: "Logic for extracting details from a specific listing"
    - path: "src/scrapers/export.py"
      provides: "Excel export functionality"
  key_links:
    - from: "src/scrapers/details.py"
      to: "Individual G-Maps listing URLs"
      via: "Scrapling visit/fetch"
    - from: "src/scrapers/export.py"
      to: "Excel File (.xlsx)"
      via: "pandas.to_excel"
---

<objective>
Implement the detailed extraction logic and data export functionality.
The goal is to extract phone, address, and recent reviews for filtered businesses and save them to Excel.

Purpose: Generate actionable lead lists for outreach campaigns.
Output: Excel file with enriched lead data (Name, Phone, Address, Reviews).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-google-maps-lead-intelligence/03-01-SUMMARY.md

# Interfaces (Planned)
- `GoogleMapsDetails.extract(listing_url: str) -> dict` (name, phone, address, reviews)
- `ExcelExporter.save(data: List[dict], filename: str) -> str`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detailed Attribute Extraction (Phone, Address, Reviews)</name>
  <files>src/scrapers/details.py</files>
  <action>
    - Implement the `GoogleMapsDetails` extractor using the Scrapling engine.
    - Visit each listing URL provided by Plan 01.
    - Implement reliable selectors for:
      - Business Name
      - Phone Number
      - Physical Address
      - Recent Reviews (Capture text of the last 3-5 reviews if available).
    - Handle cases where attributes might be missing (use nulls/placeholders).
  </action>
  <verify>
    <automated>python src/scrapers/details.py --test-extract "https://www.google.com/maps/place/..."</automated>
  </verify>
  <done>Extraction logic accurately captures phone, address, and reviews.</done>
</task>

<task type="auto">
  <name>Task 2: Excel Export & Data Structuring</name>
  <files>src/scrapers/export.py</files>
  <action>
    - Implement `ExcelExporter` in `src/scrapers/export.py` using `pandas` and `openpyxl`.
    - Format lead data into a table with headers: Name, Website (Empty), Phone, Address, Recent Reviews.
    - Ensure the filename includes a timestamp or the search keyword for tracking.
    - Save the output to a specified directory (e.g., `exports/`).
  </action>
  <verify>
    <automated>python src/scrapers/export.py --test-data '[{"name": "Test", "phone": "123"}]' && test -f exports/test_export.xlsx</automated>
  </verify>
  <done>Lead data successfully exported to Excel format.</done>
</task>

</tasks>

<verification>
- Run extraction for 5 leads and check if phone and address match Google Maps.
- Verify the exported Excel file opens correctly and shows all columns.
</verification>

<success_criteria>
- Phone, address, and reviews are extracted for at least 80% of visited listings.
- Excel file is generated with correct formatting.
</success_criteria>

<output>
After completion, create `.planning/phases/03-google-maps-lead-intelligence/03-02-SUMMARY.md`
</output>
